import React from 'react';
/*
 * Settings menu
 * TODO: use icon instead of 'settings' text
 */
const SettingsMenu = (props) =>
    <span {...props}>About | Settings</span>;

module.exports = SettingsMenu;
