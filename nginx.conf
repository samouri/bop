events {
  worker_connections  1024;  ## Default: 1024
}

http {
    include /etc/nginx/mime.types;
    server {
        server_name _;
        root /usr/share/nginx/html;

        location /swagger.yaml {
            proxy_pass http://postgrest-api:3333/;
        }

        location /api/ {
            proxy_pass http://postgrest-api:3333/;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
